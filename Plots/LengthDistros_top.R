library(grid)
library(gridExtra)
library(reshape2)
library(ggplot2)

# if a library is not installed, run e.g. install.packages("gridExtra")

cc1=12

colours <- c(colors()[630], colors()[51])

plotslist <- list()

#----------------------------------------------------------------------------
